# Vietnam Visa 24 - 환경 설정 예제
# 이 파일을 .env로 복사하고 필요한 값들을 설정하세요

# ========================================
# 환경 자동 감지 시스템
# ========================================
# 기본적으로 환경을 자동으로 감지합니다:
# - 로컬 환경: MySQL 사용
# - 온라인 환경 (Replit, CodeSandbox 등): SQLite 사용

# 수동 강제 설정 (필요시에만 주석 해제):
# DB_FORCE_SQLITE=true   # 로컬에서 SQLite 강제 사용
# DB_FORCE_MYSQL=true    # 온라인에서 MySQL 강제 사용

# ========================================
# MySQL 설정 (로컬 개발용)
# ========================================
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=vietnamvisa24_db
DB_HOST=127.0.0.1
DB_PORT=3306

# ========================================
# 일반 설정
# ========================================
NODE_ENV=development
PORT=5002

# ========================================
# JWT 설정
# ========================================
# JWT_SECRET=your_super_secret_jwt_key_here
# JWT_REFRESH_SECRET=your_super_secret_refresh_key_here

# ========================================
# 파일 업로드 설정
# ========================================
# UPLOAD_DIR=uploads
# MAX_FILE_SIZE=10485760  # 10MB

# ========================================
# 이메일 설정 (선택사항)
# ========================================
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your_email@gmail.com
# SMTP_PASS=your_email_password

# ========================================
# 사용 예시
# ========================================
# 1. 로컬 MySQL 개발:
#    - DB_USER, DB_PASSWORD, DB_NAME 설정
#    - npm start
#
# 2. 로컬 SQLite 테스트:
#    - DB_FORCE_SQLITE=true 설정
#    - npm start
#
# 3. 온라인 환경 (Replit 등):
#    - 자동으로 SQLite 사용됨
#    - 추가 설정 불필요
