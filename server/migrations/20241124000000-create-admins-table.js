"use strict";

module.exports = {
  up: async (queryInterface, Sequelize) => {
    // This migration is empty - skipping
    return Promise.resolve();
  },

  down: async (queryInterface, Sequelize) => {
    // This migration is empty - skipping
    return Promise.resolve();
  },
};
